### **防抖（debounce）**

- 定义: 频繁的触发某个操作，若在指定的时间间隔内，则只会在最后一次操作后，执行对应的后续动作; 超过这个时间间隔，触发操作后会再次执行。

- 适用场景:

  - 输入框输入触发操作（搜索）
  - 按钮点击触发操作（登录、发短信等按钮避免用户点击太快，以致于发送了多次请求）
  - 输入框联想搜索，每隔一秒发送一次请求 (也可做防抖)
  - 文本编辑器实时保存，当无任何更改操作一秒后进行保存。
  - 窗口大小 Resize。只需窗口调整完成后，计算窗口大小。防止重复渲染。

- 代码实现:

  ```html
  <body>
    <input id="ipt" type="text" />
    <div id="show"></div>

    <script>
      const showText = (e) => {
        const showText = document.getElementById("show");
        showText.innerHTML = e.target.value;
      };

      const ipt = document.getElementById("ipt");
      // ipt.addEventListener('input', debounce(showText, 1000))
      ipt.addEventListener("input", debounceByArrow(showText, 1000));
    </script>
  </body>
  ```

  ```js
  function debounce(func, delay) {
    let timer = null;
    return function () {
      const context = this;
      const args = arguments;
      if (timer) {
        clearTimeout(timer);
      }
      timer = setTimeout(() => {
        func.apply(context, args);
      }, delay);
    };
  }

  // 使用ES6中的 rest 参数获取输入事件
  const debounceByES6Arrow = (fn, delay) => {
    let timer = null;
    return (...args) => {
      clearTimeout(timer);
      timer = setTimeout(() => {
        fn(...args);
      }, delay);
    };
  };
  ```

### **节流（throttle）**

- 定义: 一段时间间隔内，第一次触发完后，再此时间间隔未结束之前，触发任何操作都不会执行对应的后续动作。

- 适用场景:

  - 滚动加载，加载更多或滚到底部监听，window.onscroll 和滑到底部自动加载更多
  - 鼠标的 mousemove、mouseover
  - 浏览器播放事件，每个一秒计算一次进度信息等
  - 高频点击提交，表单重复提交

- 代码实现:

  ```html
  <style>
    #gun {
      display: flex;
      flex-wrap: wrap;
      overflow: hidden;
    }

    .wrapper {
      width: 100%;
      height: 600px;
      box-sizing: border-box;
      overflow: auto;
      margin-top: 10px;
    }

    .gun {
      border-radius: 4px;
      background-color: rgba(137, 43, 226, 0.534);
      cursor: pointer;
      flex: 0 1 calc((100% / 4) - 20px);
      /* min-width: calc((100% / 6) - 20px); */
      padding: 10px;
      margin: 10px;
      box-sizing: border-box;
      overflow: auto;
    }

    .gun:hover {
      transform: scale(1.2);
      transition: all 0.5s;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
  </style>
  <body>
    <div class="wrapper">
      <div id="gun"></div>
    </div>
    <script>
      const list = Object.keys(new Array(123).fill(1)).map((v) => v);
      const gun = document.querySelector("#gun");

      const renderListBycreateDocumentFragment = () => {
        const fragment = document.createDocumentFragment();
        list.forEach((el) => {
          const span = document.createElement("span");
          span.classList.add("gun");
          span.textContent = el;
          fragment.appendChild(span);
        });
        gun.appendChild(fragment);
      };

      const renderListBycreateElement = () => {
        list.forEach((el) => {
          const span = document.createElement("span");
          span.classList.add("gun");
          span.textContent = el;
          gun.appendChild(span);
        });
      };

      const wrp = document.querySelector(".wrapper");

      // 监听外层dom,实现滚动底部加载
      wrp.addEventListener("scroll", (e) => {
        // 实际视口的高度
        const clientHeight = e.target.clientHeight;
        // 当前从起始点到当前滚动位置的距离
        const scrollTop = e.target.scrollTop;
        // 滚动条的总长
        const scrollHeight = e.target.scrollHeight;

        if (scrollHeight == clientHeight + scrollTop) {
          console.log("滚动到底了 :>> ");
          renderListBycreateDocumentFragment();
        }
      });

      renderListBycreateDocumentFragment();
    </script>
  </body>
  ```

  - 计时器实现

  ```js
  function throttle(func, delay) {
    let timer = null;
    return function () {
      const context = this;
      const args = arguments;
      if (timer) {
        return;
      }
      timer = setTimeout(() => {
        func.apply(context, args);
        timer = null;
      }, delay);
    };
  }

  // 使用ES6中的 rest 参数获取输入事件
  const throttle = (fn, delay) => {
    let timer = null;
    return (...args) => {
      // 若当前还存在计时，则结束当前操作后续
      if (timer) {
        return;
      }
      timer = setTimeout(() => {
        fn(...args);
        timer = null;
      }, delay);
    };
  };
  ```

  - 时间戳实现

  ```js
  function throttle(func, delay) {
    let currTime = 0;
    return function () {
      const context = this;
      const args = arguments;
      const nowTime = Date.now();
      if (nowTime - currTime > delay) {
        func.apply(context, args);
        currTime = nowTime;
      }
    };
  }
  ```

### 实践

- 在 vue 中将防抖封装为指令

  ```js
  import { createApp } from "vue";
  import App from "./App.vue";

  const app = createApp(App);
  /**
   * @description 主要用于按钮频繁点击的防抖处理  v-debounce:delay=执行方法； delay默认为 500
   */
  app.directive("debounce", {
    mounted: (el, binding) => {
      el.addEventListener("click", () => {
        const delay: number = (binding.arg && parseInt(binding.arg)) || 500;
        el.disabled = true;
        setTimeout(() => {
          el.disabled = false;
          binding.value();
        }, delay);
      });
    },
    beforeUnmount(el) {
      el.removeEventListener("click", () => {});
    },
  });
  ```
